---
name: function-testing
description: 根据需求文档（PRD）、Git 提交记录或用户故事生成功能测试用例，输出 Excel 报告。
---

# 功能测试技能

## 工作流程

1. **分析输入材料** → 识别类型，提取关键信息
2. **提取功能点** → 拆解为可测试单元
3. **设计测试用例** → 覆盖正向、异常、边界、状态切换、数据一致性
4. **生成测试报告** → 输出 Excel

---

## 用例设计原则

### 去重规则

- 不重复验证同一断言
- 不拆分单步操作，完整流程作为一条用例
- 预期结果不写模糊描述

### 粒度控制

- 不过度拆分：同一机制/流程不分散为多条用例
- 不浅层验证：不仅验证存在性，需验证数据正确性与状态联动
- 用例数量要克制，方向要精准，没有专职测试，开发自测为主
- P0（必测）**：只覆盖「出错了用户一定会发现、或会造成线上事故」的场景
- P1（应测）**：覆盖「出错了影响体验但不致命」的场景
- P2（有空再测）**：极端边界

### 场景覆盖 checklist（按需选择，非强制全选）

根据功能重要性和风险判断，按需覆盖：
- [ ] **正向**：正常流程走通
- [ ] **异常**：接口失败、网络异常、权限不足、非法输入
- [ ] **边界**：最小值、最大值、临界值、空值、超长值
- [ ] **状态切换**：状态变更后的联动更新
- [ ] **数据一致性**：多端/多入口数据同步
- [ ] **兼容性**：不同设备、系统版本、网络环境

## Excel 报告结构

| 工作表 | 字段 |
|-------|------|
| **测试概述** | 测试范围、版本信息、测试人员、测试日期 |
| **功能点清单** | 功能点ID、功能名称、优先级、实现状态、验证结果、备注 |
| **测试用例** | 用例ID、功能点ID、用例标题、前置条件、测试步骤、预期结果、优先级（P0，P1，P2）、通过（下拉：是/否）、备注 |

### 样式规范

- 表头：加粗、深色背景、白色字体
- 单元格：自动换行、垂直居中、边框线
- 列宽：ID 列窄、步骤/结果列宽
- 首行冻结：冻结表头行
